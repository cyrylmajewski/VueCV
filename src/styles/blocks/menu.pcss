.menu__list {
    display: flex;
    justify-content: center;
}

.menu__list {
    text-decoration: none;
    white-space: nowrap;
}

.menu__item {
    margin-right: 63px;
    &:last-child {
        margin-right: 0;
    }
}

.socials__icon {
    width: 25px;
    height: 25px;
    fill: $text-color;
}
